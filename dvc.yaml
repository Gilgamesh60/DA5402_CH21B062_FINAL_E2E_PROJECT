# DVC pipeline — the DAG that doubles as our CI pipeline.
# Stages are stubs in Phase 0; each phase fills in its own.

stages:
  ingest:
    cmd: echo "TODO Phase 2 - run src/ssa_ingestion"
    # deps:
    #   - src/ssa_ingestion
    # params:
    #   - ingestion
    # outs:
    #   - data/raw

  validate:
    cmd: echo "TODO Phase 2 - schema + quality checks"
    # deps:
    #   - data/raw
    #   - src/ssa_ingestion/validation.py
    # params:
    #   - validation
    # outs:
    #   - data/interim/validated.parquet

  eda_baselines:
    cmd: echo "TODO Phase 2 - produce artifacts/baselines.json"
    # deps:
    #   - data/interim/validated.parquet
    #   - notebooks/eda.ipynb
    # outs:
    #   - artifacts/baselines.json

  features:
    cmd: echo "TODO Phase 3 - build features"
    # deps:
    #   - data/interim/validated.parquet
    #   - src/ssa_features
    # params:
    #   - features
    # outs:
    #   - data/processed/features.parquet

  train:
    cmd: echo "TODO Phase 4 - train + log to MLflow"
    # deps:
    #   - data/processed/features.parquet
    #   - src/ssa_model
    # params:
    #   - training
    # outs:
    #   - artifacts/model_uri.txt
    # metrics:
    #   - artifacts/metrics.json

  evaluate:
    cmd: echo "TODO Phase 4 - evaluate + register"
    # deps:
    #   - artifacts/model_uri.txt
    # params:
    #   - evaluation
    # metrics:
    #   - artifacts/eval_metrics.json:
    #       cache: false
