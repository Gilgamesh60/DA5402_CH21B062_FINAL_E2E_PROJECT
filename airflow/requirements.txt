# Runtime deps Airflow needs to import and run ssa_ingestion.
# Pinned loosely — Airflow's constraints will resolve compatible versions.
pandas>=2.0
pyarrow>=14
pydantic>=2.5
pyyaml>=6.0
structlog>=24.1
scikit-learn>=1.4
joblib>=1.3
numpy>=1.26
scipy>=1.12
matplotlib>=3.8
mlflow>=3.0
