# Architecture Decision Records (ADRs)

One-page records of architectural choices we can't easily reverse, with
rationale. Useful when defending decisions in the viva.

Format: each ADR has a short context, the decision, and the tradeoffs.
